const buyButton = document.querySelector('.buy_button');
const loginButton = document.querySelector('.login_button');
const makeIdButton = document.querySelector('.makeid_button');

buyButton.addEventListener('click', () => {
    alert('Hello');
});